# 联机逻辑报告 · 现状与旧版对比（v183 定稿）

> 生成时间：2026-08-01 · 基线版本：v183
> 结论先行：**跨网低延迟直连已达成（用户实测 20-50ms），架构从 v148 的"完全连不上"到 v183 的"进得去 + 打得爽 + 退可守"，是 TURN 配置修对 + 穿透机制优化 + 保进房兜底三者的合力。**

---

## 一、当前联机架构（v181 定稿，分层视图）

```
┌─────────────────────────────────────────────────────────┐
│ 流程层 (main.js)                                          │
│  握手幂等(v181) / 选风格确认无限重发+UI自愈(v183)           │
│  开战守卫(isP2PReady) / 标签如实显示(TURN中继/直连)          │
├─────────────────────────────────────────────────────────┤
│ 健壮层 (net.js)                                          │
│  信令超时兜底 30s(v182) / P2P无限重试(前2轮8s后20s, v178)    │
│  RTT限速(>150ms→15Hz, v177) / 序号去重(_acceptSeq)          │
├─────────────────────────────────────────────────────────┤
│ 传输层 (net.js)                                          │
│  ┌─ P2P主通道: PeerJS(WebRTC DataChannel, reliable)       │
│  │   STUN国内优先(v173) + Metered TURN动态拉取(jp, v155)   │
│  ├─ TURN兜底: 对称NAT时ICE自动走relay(~220ms)              │
│  └─ MQTT保进房(v181): P2P 20s未握手才启动                  │
│      仅承载握手+低频控制消息, state/input永不走MQTT          │
└─────────────────────────────────────────────────────────┘
```

**职责边界（关键）**：
- **对局数据（state/input）**：永远只走 P2P/TURN 通道 → 直连 20-50ms / 中继 220ms 基线，**绝无 500ms+ 尖刺**（v167 删 MQTT 对局通道 + v177 RTT 限速的双重保障）
- **进房/握手**：P2P 20s 内失败 → MQTT 兜底走通（跨网必能进）
- **低频控制消息**（开始/选风格确认/再来一局）：双通道冗余 → 关键流程可靠送达

---

## 二、与旧版完整对比（按用户记忆的关键节点）

| 维度 | v110（"以前能玩"） | v145 方案X | v167（删MQTT） | **v181/v183（现在）** |
|---|---|---|---|---|
| 通道策略 | P2P优先 + MQTT并行备用 | P2P一次决策 + 中继显式兜底 | 纯 P2P/TURN | **P2P/TURN 对局 + MQTT 仅保进房** |
| TURN | openrelay（**已停服**） | Metered 但**域名填错** | Metered jp 动态拉取（v155 修对） | 同 v155（**动态拉取 + 凭据实测有效**） |
| 保进房 | ✅ MQTT 20s 兜底 | 显式切换（弱） | ❌ **无**（跨网全靠 TURN 一条腿） | ✅ **MQTT 20s 兜底恢复** |
| 对局数据通道 | 可能走 MQTT（800ms 尖刺） | 单通道 P2P | 纯 P2P | **纯 P2P/TURN（永不 MQTT）** |
| 防延迟堆积 | ❌ | ❌ | ❌ | ✅ RTT 限速（v177） |
| 信令超时兜底 | ❌ | ❌ | ❌ | ✅ 30s（v182） |
| 握手 | v121 handshaked | ✅ | ✅ | ✅ + 幂等（v181） |
| 协商窗口 | 一次 connect + 20s | 无重试 | 8s 重开（v163） | ✅ 前2轮8s + 20s 长轮（v178） |
| STUN | Google×5+Twilio+QQ+MiWiFi | 同 | +Metered | ✅ 国内优先（v173） |
| 连接标签 | 无 | 无 | getChannelDetail | ✅ 真实通道（v164/165） |

**核心差异结论**：
1. **v110"能进"** = MQTT 保进房兜底（与延迟无关）
2. **v167"进不去"** = 删 MQTT 时把保进房也删了，跨网全靠 TURN
3. **v181 恢复** = 保进房兜底回归，且对局数据仍纯直连（吸收两代优点）

---

## 三、成功原因总结（为什么现在跨网能低延迟）

1. **TURN 配置修对（v148→v155，六版试错）**：正确域名 `sg.relay.metered.ca` + 有效凭据（Allocate 实测）+ 动态拉取（凭据不失效）→ **TURN 兜底第一次真正活着**，对称 NAT 也能连
2. **单连接协商 + 长宽限（v162/163/178）**：ICE 协商不再被重试打断，慢速场景（TURN 分配 8-20s）也能完成 → **穿透成功率最大化**
3. **国内 STUN 优先（v173）**：srflx 公网候选收集更稳
4. **RTT 限速（v177）**：中继延迟打回链路基线（220ms），不滚雪球到 1000+
5. **MQTT 保进房（v181）**：跨网必能进房间走流程
6. **直连本身是概率事件**：运营商 CGNAT 映射动态变化——用户多次实测（10:34 / 13:15 / 13:45 直连成功）证明当前网络组合**直连成功率不低**

---

## 四、可优化点排查（诚实清单）

| # | 优化点 | 现状 | 可优化空间 | 建议 |
|---|---|---|---|---|
| 1 | **中继延迟 220ms** | jp 节点物理距离 | **大**（可到 10-30ms） | 近岸 coturn（国内/香港，需服务器+实名）——唯一大项，用户已知 |
| 2 | 直连概率 | 代码已拉满（STUN/协商/重试） | 小 | 剩余由运营商 NAT 决定，代码无能为力 |
| 3 | 标签 2-6s 滞后 | 已知 | 小 | 以延迟数字为准（20-50=直连），无害 |
| 4 | TURN 凭据明文 | 公开仓库可见 | - | Metered 后台可随时 Revoke（已知） |
| 5 | 中继高延迟时提示重连 | 无 | 中（体验） | 可加"当前中继延迟高，建议断开重连再抽签"提示（纯 UI，不动底层） |
| 6 | 连接后 relay→direct 升级 | 无（v179 自动重抽已回退） | 小 | **不建议自动**（v179 教训：打断流程）；手动重连即重抽签 |

**结论：代码层无重大遗漏。** 唯一实质性优化（近岸 TURN）受限于实名/服务器，已给用户完整方案（`deploy/turn/setup.sh` 一键脚本已备）。

---

## 五、"运气"验证方案（1-2 小时后复测用）

**判断标准**：直连是概率事件，用**多次采样**区分"运气好"还是"机制稳"：

| 测试方法 | 判定 |
|---|---|
| 同组合连 5-10 局，记录每局标签（P2P直连 / TURN中继）+ 延迟 | 直连率 ≥ 50% → 机制稳定；< 30% → 当日网络偏好 |
| 覆盖不同时段（现在 / 2小时后 / 晚上） | 多次时段都高 → 非运气 |
| 抽中中继时，关飞行模式重连 1-2 次 | 重抽后直连概率上升 → 正常抽签行为 |

**预期**：结合实测（一天内多次直连成功），你的组合直连率大概率在 30-60% 区间波动，属正常；中继时 220ms 也能玩（v177 防堆积）。

---

*报告基于 v183 实际代码 + git 历史（v110: 8564232 / v121: ad13f9d / v145: 5383927 / v167 / v181 / v183）逐项核对。联机底层已冻结，本报告不构成改动依据，仅记录现状与依据。*
