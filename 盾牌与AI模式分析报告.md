# PIXEL MECH ARENA · AI 对战模式 & 盾牌机制 详细报告

> 数据来源：`js/game.js`（AI 决策 `makeAI` + 战斗结算 `_resolveCombat`）、`js/player.js`（机甲 `Mech` 类 `takeHit` / `startAttack` / 状态机）。
> 数值为当前线上 v124 实测代码，非设计文档推演。

---

## 一、六种 AI 风格（AI 对战观战）区别

每个风格是一组决策参数，由 `makeAI(cfg)` 驱动一个加权随机状态机：每隔 `reactTimer` 秒重掷一次决策。
`reactTimer = reactMin + 随机×(reactMax − reactMin)`，即**反应延迟区间，越小越果断**。

决策逻辑（按优先级）：
1. 对手正在攻击 且 距离 < 90 → 以 `defendProb` 概率进入「防御」。
2. 距离 > `approachDist` → 「接近」。
3. 距离 < `closeDist`（贴脸）→ 按 `closeL : closeH : closeBack` 加权抽「轻击 / 重击 / 后撤」。
4. 中间距离 → 按 `midApproach : midL : midDef` 加权抽「接近 / 轻击 / 防御」。
5. 若 `jumpProb > 0` 且在地，偶尔「跳跃」。

| 风格 | 反应延迟 | 防御倾向 | 接敌距离 | 贴脸倾向 | 核心性格 |
|---|---|---|---|---|---|
| **均衡 balanced** | 0.25–0.55s（中） | 0.60（常防） | 130（中） | 60（中） | 全能型，反应中庸，被攻击时 60% 防御，偏好拉扯接近，贴脸多以轻击+后撤 |
| **猛攻 berserker** | 0.15–0.35s（快） | 0.25（极少防） | 150（远也冲） | 50（极贴） | 玻璃大炮rush，反应快、几乎不防，重击占比高(closeH 0.45)，贴脸就干 |
| **铁壁 bulwark** | 0.20–0.40s（中） | 0.85（最常防） | 120（略远） | 70（稍远） | 龟缩反击，被攻击 85% 防御，贴脸重击极少(closeH 0.10)、常后撤，中距也常防(0.40) |
| **游击 skirmisher** | 0.20–0.45s（中） | 0.40（中） | 140（中远） | 55（贴） | 放风筝，贴脸后撤概率最高(closeBack 0.40)，中距也常后撤，偶尔跳(0.10) |
| **疾风 gale** | 0.15–0.35s（快） | 0.30（低防） | 150（远也冲） | 60（中） | 空中游击，反应快、低防，轻击占比最高(closeL 0.60)，**跳跃概率全场最高 0.35**，极难被钉住 |
| **精准 precision** | 0.10–0.25s（**最快**） | 0.75（常防） | 130（中） | 65（中） | 读招惩罚型，反应最快（抓帧惩罚），防御高(0.75)，攻防均衡，克制可预测的进攻者 |

**关键参数逐风格速查**（轻击/重击/后撤 = closeL/closeH/closeBack；中距接近/轻击/防御 = midApproach/midL/midDef）：

- balanced：0.50 / 0.20 / 0.30 ｜ 0.60 / 0.25 / 0.15 ｜ jump 0
- berserker：0.35 / 0.45 / 0.20 ｜ 0.70 / 0.25 / 0.05 ｜ jump 0.05
- bulwark：0.30 / 0.10 / 0.30 ｜ 0.40 / 0.20 / 0.40 ｜ jump 0
- skirmisher：0.40 / 0.20 / 0.40 ｜ 0.50 / 0.35 / 0.15 ｜ jump 0.10
- gale：0.60 / 0.20 / 0.20 ｜ 0.70 / 0.25 / 0.05 ｜ jump 0.35
- precision：0.55 / 0.25 / 0.20 ｜ 0.55 / 0.25 / 0.20 ｜ jump 0.05

**对战相克直觉**：猛攻/疾风（低防高攻）会被铁壁/精准（高防）克制；铁壁（极少进攻）怕耐心拉扯放风筝的游击；精准（最快反应）专抓猛攻/疾风的出招后摇。

---

## 二、盾牌 / 防御 当前全部信息（v124）

### 2.1 触发与状态
- **按住**防御键（非点按）。进入条件：`onGround` 且不在攻击中（`player.js` 156–159）。
- 防御中 `defending=true`、状态 `defend`。**防御时不能移动、不能攻击、不能跳跃**（`moveLocked = defending ‖ attacking`，且 `startAttack` 内 `if (this.defending) return false`）。
- 每帧 `defending` 先复位为 false，再据按键重设——即**松手即解除**，下一帧即可行动。

### 2.2 减伤与反弹数值（`takeHit` + `_resolveCombat`）【v128 已落地】
- 减伤：**承受 `dmg × 0.1`，即减伤 90%**（内部保留小数，显示四舍五入）。仍受小幅击退 `vx += 0.6`，**不进入 hurt 状态**。
- 反弹：被防御时 `result < 0`，`reflected = Math.round(hb.dmg × 0.6)` → **反伤攻击者 60%（按攻击方原始伤害算）**。
- 当前实战推演（攻击方→防御方）：
  - 轻击 dmg 8：防御方 −0.8，攻击方 −4.8（被反伤）。**防御方净赚 4.0**。
  - 重击 dmg 18：防御方 −1.8，攻击方 −10.8（被反伤）。**防御方净赚 9.0**。

### 2.3 后摇与冷却（v128 已落地）
- **后摇（aftercast）：无。** 松手防御的下一帧即可移动/攻击/跳跃，无锁定。
- **再防冷却（v128 新增）：0.5s。** 松手防御后进入 `defendCD` 计时，0.5s 内不能再次举盾（`player.js` 新增 `defendCD` 字段；攻击中/离地/硬直时不能举盾）。这打破了"无限举盾"——每次防御→松手都有约 0.5s 破绽，进攻方可"骗防→等冷却→惩罚"。
- 攻击后摇对照（供参考）：轻击 0.14s、重击 0.40s（即 `cd` 字段）。

### 2.4 与其他系统交互
- 防御中受伤不进 hurt，故不会被连段打断硬直。
- 反弹伤害直接扣攻击方 HP，攻击方若因此归零会进入 KO。
- 联机：防御状态 `defending` 随 state 广播，观战/客户端镜像一致；减伤与反弹在 **host 权威结算**，客户端只看结果。

---

## 三、【已落地 v128】最终采用的盾牌改动：减伤 90% + 反伤 60% + 0.5s 再防冷却

> 原评估章节（减伤100%+反伤100%+后摇+冷却）的推演与风险分析仍成立，作为"若未来进一步上调"的参考保留在下方第四节。最终落地为更稳妥的 **90/60 + 0.5s 再防冷却**（你本轮明确指定的数值）。

### 3.1 落地数值推演
- 减伤 90%：防御方承受 `dmg × 0.1`。
- 反伤 60%：`reflected = Math.round(hb.dmg × 0.6)`。
- 改动后（攻击方→防御方）：
  - 轻击 8：防御方 −0.8，攻击方 −4.8。**防御方净赚 4.0**（原 2.4，约 1.67×）。
  - 重击 18：防御方 −1.8，攻击方 −10.8。**防御方净赚 9.0**（原 5.4，约 1.67×）。

### 3.2 为何不取 100/100
- 90/60 已显著强化防御收益（净赚约 1.67×），但**保留了进攻价值**：被防下的重击攻击方只亏 10.8（而非 18），不会出现"进攻=送死"导致双盾 DRAW 横行。
- 0.5s 再防冷却打破了"无限举盾"，制造约 0.5s 破绽窗口，进攻方可用"骗防→等冷却→惩罚"破解，形成博弈。
- 若后续实测防御仍过强，再上探反伤至 80%、或补后摇 0.25–0.40s（实现要点见第四节 3.3）。

### 3.2 平衡影响
1. **进攻风险陡增**：任何被防下的攻击，攻击方反亏自己全额伤害，防御方零损。进攻型风格（猛攻/疾风）一旦被读防即崩盘。
2. **防御成"免费赢"**：因防御零损，唯一风险来自你主动松手的窗口。**若后摇/冷却不够大，防御将不可战胜**，进攻毫无价值。
3. **双盾僵局**：双方都按住防御→互不掉血→60s 到点比血（均满）→**DRAW**。当前无"破防/削血(chipt dmg)"机制，无限按住不会触发任何惩罚（后摇/冷却只在"松手"时触发，持续按住反而绕开）。
4. **对 AI 风格放大**：高 `defendProb` 的 铁壁(0.85)/精准(0.75) 会高频防御并全额反伤，观战里这类房主/客户端将极其难缠。

### 3.3 实现要点（若采纳）
- **减伤 100%**：`takeHit` 内 `const taken = dmg * 0.2` → `0`。
- **反伤 100%**：`reflected = Math.round(hb.dmg * 0.5)` → `Math.round(hb.dmg)`。
- **后摇**：防御 `defending` 由 true→false 的瞬间，令 `this.cooldown = 后摇值`（复用现有 cooldown 即锁住攻击，实现"松手后短暂不能攻"）；若要同时锁移动，需让 `moveLocked` 也包含 `cooldown > 0`。
- **冷却（不能立刻再防）**：新增字段如 `blockLock`，松手时置位、逐帧递减，并在进入防御处加 `if (this.blockLock > 0) return` 门槛。

### 3.4 建议
- **后摇/冷却是必选项，不是可选项**：没有它们，100/100 直接破坏攻防平衡。
- **建议量级**（供 playtest）：后摇 0.25–0.40s（松手后短暂不能攻/动）+ 再防冷却 0.5s 左右（松手后不能立刻重新举盾）。这样防御方每次举盾→松手有约 0.5–0.8s 破绽，进攻方可用"骗防→等后摇→惩罚"破解，形成心理博弈（类真实格斗游戏 blockstun/recovery）。
- **反伤 100% 偏激进**：若实测进攻被完全压制，可退到反伤 60–80%（仍强于当前 50%，但保留进攻价值）。减伤 100% 本身风险可控（配合后摇即可）。
- **防双盾僵局**：若实测大量 DRAW，可加"持续举盾超时强制破防/微量削血"或"到点比血改判进攻积极性"。
- **务必先在 AI 房 playtest**：重点看 铁壁/精准（高防）是否变成"举盾反弹怪"，以及双盾是否频繁 DRAW。

> 结论：**方向可行，但必须把"后摇 + 再防冷却"做实且给够时长**，否则 100/100 会让进攻失去意义、观战变成举盾比拼。建议先以 减伤100% + 反伤80% + 后摇0.3s + 再防冷却0.5s 起调，再按手感收反伤。
