# PIXEL MECH ARENA · 团队技术提升与代码质量门禁

> 资深开发工程师视角。本文是给本团队协作（含帮手）的代码评审标准与架构认知基线，
> 不是一次性建议，而是每次改代码前该对照的清单。

---

## 一、代码评审铁律（最高优先级）

帮手曾基于独立副本报 8 个 bug，逐条核对后 **7 个误报 + 1 个可忽略**——根因是没在最新代码上复现。

**改代码 / 报 bug 前，必须做到：**

1. **先 `git pull` 跑最新代码**，再谈任何"问题"。
   你看到的现象若来自旧副本 / 另一工作区，一律视为"未验证"，不算 bug。
2. **报 bug 必须能复现**。
   纯静态分析（"这里引用了 X，可能报错"）不算数——必须在实际运行环境（浏览器/真机）看到报错，或能指出确切的执行路径。
   - 反面教材：`IS_TOUCH_UI is not defined` 报告。实际 `game.js:84` 是**顶层 `const`**，且 `game.js` 在 `main.js` 之前以经典 `<script defer>` 加载，两者共享**全局词法作用域**，根本不会 ReferenceError。报告者没确认加载顺序就下了结论。
3. **改动前读懂分层**，不要"哪里不对改哪里"。
   任何一个文件改之前，先回答三个问题：它在哪个层？它依赖什么？谁调用它？答不上来就先读，别盲改。

---

## 二、本项目必懂的架构分层

### 1. 联机网络：P2P 优先，中继仅保联机房（v109 定型）
- **P2P（PeerJS/WebRTC）**：同 WiFi/局域网 10~30ms；跨网经免费 TURN（openrelay.metered.ca）走真·P2P，延迟远低于境外 MQTT broker。
- **中继（MQTT over WebSocket）**：EMQX/HiveMQ 公共 broker，海外节点，**实测 800ms**——只能当"保联机房"兜底，**绝不能当首选**。
- **双通道并行 + 序号去重**：`send()` 永远 P2P 优先；P2P 与中继同发同一条消息、带同一 `sendSeq`，接收端 `_acceptSeq` 丢弃重复/过期包，避免画面回跳或重复出招。
- ⚠️ **历史坑（已修）**：旧代码 host 一旦因超时切中继，会 `c.close()` 拒收后来连上的 P2P——导致永久卡 800ms。现改为"P2P 迟到自动接管"。

### 2. 前端脚本加载：经典 `<script defer>` 的全局词法作用域
- 本项目所有 JS 用经典 `<script defer>`（非 ES module），**顶层 `const`/`let` 在所有脚本间共享全局词法作用域**。
- 所以 `game.js` 里声明的 `IS_TOUCH_UI`、`STATES`、`MODES` 在 `main.js` 里可直接访问——**不要误判为"未定义"**。
- 但也因此：新 DOM 元素的 `const` 引用必须放在文件顶部 DOM 区（v103 曾因声明在 549 行、启动时调用触发 TDZ 死锁）。

### 3. 经典 CSS 坑（改样式前必知）
- **祖先 `transform` 会让 `position:fixed` 子元素改为相对该祖先定位**（v103 fadeIn 坑）。需要 `fixed` 全屏定位的元素，祖先链上不能有 `transform`/`filter`/`perspective`——`fadeIn` 现只动 `opacity`。
- **`flex:1` 会贪婪占空间挤压兄弟按钮**（v104 坑）。输入框用 `flex:1` 时，旁边按钮必须显式定宽，否则被挤到只剩一半。
- **移动端禁滚动**：`html,body { overflow:hidden; height:100dvh }`，桌面/移动用 `@media(max-width:700px)` 与 `hover:hover` 分流。

---

## 三、性能 / UX 优先级

- **架构缺陷先修架构，不补丁式优化**。800ms 中继是架构选型问题（中继被选为首选），正确做法是"P2P 优先"——不是去优化中继延迟（那受限于公共 broker 地理位置，你控制不了）。
- **用户主战场是移动端（390×844）**。所有改动先按移动端验证通过，再考虑桌面。避免引入桌面专属布局、避免竖屏溢出。
- **60fps 与 1.5s 内可玩**是底线。状态包 30Hz、可靠通道即可，不要为"更流畅"盲目上不可靠通道（会丢 start/reset 关键控制消息）。

---

## 四、协作纪律（与帮手并行开发）

1. **改前必同步**：`git fetch` + `git ls-remote origin main` 比 SHA；云端领先就先 `pull --rebase`，绝不在落后副本上改。
2. **改完即推**：本地已配 `post-commit` 自动 push（含 main 分支守卫 + rebase/cherry-pick/merge 守卫）。帮手侧也配同类钩子。
   - ⚠️ 自动 push ≠ 自动同步："改前必同步"那步仍要手动走，否则远端领先时 push 被拒，补 `pull --rebase` 即可。
3. **版本号防撞**：bump 前以**云端实际 `#versionTag`** 为准，别在各自旧值上加。
4. **版本号规则**：用户列几条需求就加几（数子任务点数）；`?v=NN`(×12) 与 `#versionTag` 同步；CHANGELOG 仅留近 3 条且**以用户视角写实**（别写"字体脚本异步"这种技术黑话，要写"修复首页卡死/点击无响应"）。

---

## 五、给帮手的具体核查要求

- 任何反馈先说明：基于哪个 commit / 哪份代码、是否在最新 `main` 上复现。
- 纯静态分析结论必须标注"未运行验证"，且我这边会在真实代码里逐条核对后才动。
- 涉及网络/延迟的改动，先讲清当前通道选择逻辑（见第二节第 1 点），再提方案。

---

*本文随项目演进持续更新。改动网络/联机策略时，本条标准优先于临时判断。*
